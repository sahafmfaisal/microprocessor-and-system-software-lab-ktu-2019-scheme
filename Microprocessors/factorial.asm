DATA SEGMENT
    NUM     DB 04H
    RESULT  DW ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA

START:
    MOV AX, DATA
    MOV DS, AX

    MOV AL, NUM
    MOV BL, AL
    MOV AX, 1

L1:
    MUL BL
    DEC BL
    JNZ L1

    MOV RESULT, AX
    INT 3H

CODE ENDS
END START