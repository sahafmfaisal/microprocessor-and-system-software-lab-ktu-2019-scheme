DATA SEGMENT
    MSG  DB 'HELLO$'
    MSG1 DB ' WORLD$'
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA

PRINT MACRO STR
    MOV DX, OFFSET STR
    MOV AH, 09H
    INT 21H
ENDM

START:
    MOV AX, DATA
    MOV DS, AX

    PRINT MSG
    PRINT MSG1

    MOV AH, 4CH
    INT 21H

CODE ENDS
END START